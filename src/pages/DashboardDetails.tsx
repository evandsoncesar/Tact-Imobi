import Sidebar from '../components/SideBar';

export default function DashboardDetails() {
  return (
    <div style={{ display: 'flex' }}>
      <Sidebar />
      <h1 style={{marginLeft: 'auto', marginRight: 'auto', marginTop: 200, color: '#29306A'}}> Em desenvolvimento </h1> 
    </div>
  );
}
